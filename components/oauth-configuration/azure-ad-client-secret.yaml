---
# WARNING: This is a template file with placeholder values
# For security, this secret should be created using one of the following methods:
# 1. GitHub Actions with sealed secrets (recommended for GitOps)
# 2. Manual deployment with real values (not committed to Git)
# 3. External Secrets Operator with Vault integration
# 4. Helm values override during deployment
apiVersion: v1
kind: Secret
metadata:
  name: openid-client-secret-azuread
  namespace: openshift-config
  annotations:
    argocd.argoproj.io/sync-wave: "10"
    # Exclude from GitOps sync - deploy manually or via sealed secret
    argocd.argoproj.io/sync-options: "SkipDryRunOnMissingResource=true"
type: Opaque
stringData:
  # PLACEHOLDER: This value should NEVER contain real secrets in Git
  # Real value should be provided via:
  # - GitHub Secret: AZURE_AD_CLIENT_SECRET
  # - Vault path: secret/openshift/{env}/oauth/azure_ad_client_secret
  # - Manual oc command: oc create secret generic openid-client-secret-azuread --from-literal=clientSecret="real-secret-here"
  clientSecret: "PLACEHOLDER_AZURE_AD_CLIENT_SECRET_REPLACE_ME"
